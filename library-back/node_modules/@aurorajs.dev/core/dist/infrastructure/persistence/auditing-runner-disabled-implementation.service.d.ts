import { AuditingMeta, AuditingRunner } from '../../domain';
export declare class AuditingRunnerDisabledImplementationService extends AuditingRunner {
    create(auditingMeta: AuditingMeta, now: string, event: string, modelPath: string, modelName: string, auditableId: string, oldValue: any, newValue: any): void;
}

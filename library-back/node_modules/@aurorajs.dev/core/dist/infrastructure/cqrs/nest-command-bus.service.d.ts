import { CommandBus as NestCommandBusImplementation, ICommand } from '@nestjs/cqrs';
import { ICommandBus } from '../../domain/cqrs';
export declare class NestCommandBus implements ICommandBus {
    private readonly commandBus;
    constructor(commandBus: NestCommandBusImplementation);
    dispatch<T extends ICommand>(command: T): Promise<any>;
}

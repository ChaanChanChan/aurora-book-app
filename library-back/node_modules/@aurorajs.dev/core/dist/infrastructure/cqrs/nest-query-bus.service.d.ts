import { QueryBus as NestQueryBusImplementation, ICommand } from '@nestjs/cqrs';
import { IQueryBus } from '../../domain/cqrs';
export declare class NestQueryBus implements IQueryBus {
    private readonly queryBus;
    constructor(queryBus: NestQueryBusImplementation);
    ask<T extends ICommand>(query: T): Promise<any>;
}

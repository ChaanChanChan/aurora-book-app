import { ConfigService } from '@nestjs/config';
import { Cache } from 'cache-manager';
import { FormatLangCode } from '../../domain/aurora.types';
import { QueryStatement } from '../../domain/persistence';
export declare class AddI18NConstraintService {
    private cacheManager;
    private readonly configService;
    constructor(cacheManager: Cache, configService: ConfigService);
    main(constraint: QueryStatement, i18NRelation: string, contentLanguage: string, { contentLanguageFormat, defineDefaultLanguage }?: {
        contentLanguageFormat?: FormatLangCode;
        defineDefaultLanguage?: boolean;
    }): Promise<QueryStatement>;
}

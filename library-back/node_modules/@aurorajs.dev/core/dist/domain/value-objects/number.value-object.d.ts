import { ValueObject } from './value-object';
export declare abstract class NumberValueObject extends ValueObject<number | null | undefined> {
    set value(value: number | null | undefined);
    get value(): number;
    toString(): string;
}

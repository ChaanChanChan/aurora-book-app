import { NumberValueObject } from './number.value-object';
export declare abstract class IntValueObject extends NumberValueObject {
    set value(value: number | null | undefined);
    get value(): number;
    toString(): string;
}

import { NumberValueObject } from './number.value-object';
export declare abstract class DecimalValueObject extends NumberValueObject {
    set value(value: number);
    get value(): number;
    toString(): string;
    decimalCount(value: number): {
        integers: number;
        decimals: number;
    };
}

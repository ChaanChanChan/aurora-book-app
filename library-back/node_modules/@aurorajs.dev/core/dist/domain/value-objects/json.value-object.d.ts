import { ValueObject } from './value-object';
export declare abstract class JsonValueObject<T = string | object> extends ValueObject<T> {
    set value(value: any);
    get value(): any;
    toString(): string;
}

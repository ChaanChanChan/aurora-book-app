/// <reference types="node" />
import * as sharp from 'sharp';
export declare class ImageManager {
    static dimensions(path: string): Promise<{
        width: number;
        height: number;
    }>;
    static exif(prop: string | Buffer): Promise<any>;
    static isImageMime(mime: string): boolean;
    static loadImage(path: string): Promise<sharp.Sharp>;
    static changeFormat(image: sharp.Sharp, format: string, options?: sharp.PngOptions | sharp.JpegOptions): sharp.Sharp;
    static crop(image: sharp.Sharp, region: sharp.Region): sharp.Sharp;
    static resize(image: sharp.Sharp, width?: number | null, height?: number | null, options?: sharp.ResizeOptions): sharp.Sharp;
    static cropAttachment(attachment: any, attachmentFamily: any, crop: any): {
        attachment: string;
        attachmentFamily: string;
        crop: string;
    };
}
